import { createTheme } from "@mui/material/styles";

const AppTheme = createTheme({
  palette: {
    primary: {
      main: "#21A696",
    },
    text: {
      primary: "#333333",
      secondary: "#808080",
    },
  },
});

export default AppTheme;
